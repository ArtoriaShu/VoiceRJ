# No Voice：产品与设计规格

## 产品目标

No Voice 是私有的本地音声资料库。它在一处管理本地目录、作品、音轨与播放进度，并提供一个可选的外部作品发现区。应用不承担购买、购物车、账户或第三方社交功能。

## 当前页面结构

1. 固定左侧导航：资料库、发现、最近播放、本地目录和偏好设置。搜索框与筛选入口也属于左侧栏，位于主导航上方，采用 Apple Music Web 的侧栏工具区位置。
2. 主内容区：不显示“资料库”大标题，也不显示“X 个本地目录”。其后依次为“人气作品”“最新上架”两条横向媒体轨，以及本地资料库。
3. 右侧作品抽屉：展示本地作品的音轨并选择播放。
4. 悬浮底部播放器：始终相对浏览器窗口水平居中，仅服务于本地音频播放。
5. “X 个本地目录”、目录列表、添加目录、扫描、启用/停用和错误状态只显示在“本地目录”管理界面，不能占用发现页首屏。

## 视觉系统

- 采用 Apple Music Web 式桌面媒体库信息架构：固定侧栏、可纵向滚动的内容区、横向作品轨与持久播放器。
- 侧栏位于最高层，使用半透明背景、背景模糊、轻描边与内高光。横向作品轨必须可以从其后方穿过；卡片经过时由侧栏的毛玻璃产生遮罩效果。
- 发现轨的卡片为严格 **4:3**，使用清晰的原始尺寸封面，不允许拉伸或 240px 缩略图放大。高度与 Apple Music Web 的发现轨保持克制：首屏应保留下一行内容预览，不让单条作品轨占满整个视口。
- 卡片的来源/标题/作者放在封面上方；封面底部只显示简短说明。悬停时卡片轻微上浮、封面缓慢放大、底部播放提示淡入。必须遵守 `prefers-reduced-motion`。
- 字体为系统 San Francisco / 苹方优先，标题使用紧凑字距和克制字重；界面以白、浅灰、墨黑和红色强调态构成，不使用粗重边框或夸张渐变。
- 悬浮播放器必须以整个视口为基准居中，而不是在主内容区内居中；背景为可读的毛玻璃层。

## 发现轨：后续功能

当前顶部内容使用中性的高分辨率专辑图与名称，仅用于视觉演示。接入功能时替换为服务端缓存的公开结果，不把外部作品伪装成本地音频。

| 轨道 | 数据源 | 排序 |
| --- | --- | --- |
| 人气作品 | https://www.dlsite.com/maniax/fsr/=/language/jp/sex_category[0]/male/ana_flg/all/order/trend/work_type_category[0]/audio/genre[0]/048/options_and_or/and/options[0]/JPN/options[1]/NM/lang_options[0]/%E6%97%A5%E6%9C%AC%E8%AA%9E/lang_options[1]/%E8%A8%80%E8%AA%9E%E4%B8%8D%E8%A6%81 | `trend` |
| 最新上架 | https://www.dlsite.com/maniax/fsr/=/language/jp/sex_category[0]/male/ana_flg/all/order/release_d/work_type_category[0]/audio/genre[0]/048/options_and_or/and/options[0]/JPN/options[1]/NM/lang_options[0]/%E6%97%A5%E6%9C%AC%E8%AA%9E/lang_options[1]/%E8%A8%80%E8%AA%9E%E4%B8%8D%E8%A6%81 | `release_d` |

实现要求：

- 只由服务端请求、清洗、缓存公开元数据；浏览器不直连第三方，也不发送用户 Cookie。
- 每条轨独立缓存、独立显示更新时间、加载、空结果与失败状态；不能混用本地 `works` 数据。
- 解析并保存 `sourceId`、标题、来源 URL、原始封面 URL、社团、预售提示等必要字段；忽略跟踪属性、购物车、收藏和所有站内操作。
- 封面使用原始 `modpub` 资源或等效的高分辨率原图，后端应缓存并校验来源域名、大小与超时。
- 外部作品只能打开预览/来源或触发明确的导入动作；没有本地音频时绝不能播放。

## 本地资料库功能

- 添加、启用、停用、删除本地目录，并增量扫描目录。
- 识别作品、封面、音轨及缺失文件；支持作品、RJ 编号、社团搜索。
- 音频端点支持 Range 请求；详情抽屉能选择音轨，播放器可播放、暂停并记录进度。
- 作品封面缺失时可显示低调的本地占位图，但不影响浏览与播放。

## 本地扫描与文件匹配

- 用户可选择一个或多个根目录。扫描器递归遍历其子目录，识别支持的音频文件，并建立“根目录 → 作品目录 → 音轨”的可增量更新索引。
- 音频文件至少支持 `mp3`、`m4a`、`flac`、`wav`、`ogg`、`opus`、`aac`；扫描必须跳过隐藏文件、符号链接越界、不可读文件和根目录之外的路径。
- 同时扫描字幕文件 `lrc`、`vtt`、`srt`，以及常见封面文件。字幕优先按与音轨同名的基名匹配；无法自动匹配时，在作品详情页提供明确的手动关联动作。
- 卡片和详情的封面容器固定为 **4:3**。DLsite 原始封面应优先使用其原图（例如 `modpub` 资源），不放大 240px 缩略图；若本地封面为方图，则在 4:3 容器中以不拉伸的裁切策略显示。

## DLsite 与 ASMR.ONE 元数据同步

每个本地作品保留可编辑的 `rjCode`。详情页提供“同步 DLsite 数据”“同步中文标题”两个明确按钮，并展示最后同步时间、来源、成功/失败状态；同步不能在页面加载时自动发生。

### DLsite 同步

- 使用 RJ 编号获取公开元数据：原始标题、社团、原始封面、来源 URL、可用的发布日期/时长等。
- 请求、HTML 解析、缓存与错误处理均在服务端完成；前端不直接抓取第三方，不携带 Cookie，不解析或渲染未清洗 HTML。
- 标题优先级固定为：`manualTitle`（用户手动标题） > `asmrOneTitleNormalized`（ASMR.ONE 中文标题） > `dlsiteTitle` > `folderName`。

### ASMR.ONE 中文标题同步

- “同步中文标题”根据 `https://www.asmr.one/work/<RJ编号>` 获取公开内容，保存来源 URL 与同步时间。
- 用户提供的结构中，中文标题来自页面首个主 `h1`；社团可取标题下的作者/社团文本；DLsite 链接仅用于校验 RJ 对应关系。时长、带字幕、多语种和页面标签均为可选元数据，缺失时不得报错。
- 解析器只读取清洗后的纯文本与已校验的 HTTPS URL。忽略评分控件、下载、播放列表、评论、追踪属性、站内操作按钮及所有不需要的 HTML。
- 同步结果必须先进行标题规范化，再写入 `asmrOneTitleNormalized`。如果作品没有 RJ 编号、编号不匹配、页面不可达或解析不到 `h1`，显示可恢复的失败信息，不覆盖现有标题。

### 标题规范化与手动编辑

- 自动移除**仅出现在标题开头且可重复出现**的语言标记，包括 `简体中文版`、`繁体中文版`、`日本語版`、`日本語`、`English ver.`、`English Version` 及其全角/半角圆括号、方括号、书名号式括号组合，例如：`【简体中文版】【NTR】标题` 应保留为 `【NTR】标题`。
- 不删除标题中部或末尾的文字，不删除非语言题材标签，不把清洗失败的空字符串写回数据库。
- 作品详情必须有“编辑标题”动作：显示当前生效标题、可保存 `manualTitle`、可取消、可恢复到同步标题；保存后立即更新卡片、搜索索引和详情标题。

## 字幕、歌词与桌面歌词

### 格式与数据模型

- 支持读取和导入 `.lrc`、`.vtt`、`.srt`：保留原始文件、字符编码处理结果、语言、来源和与音轨的关联。
- 统一解析为时间轴片段：`startMs`、`endMs`、`text`。LRC 的单时间戳行以下一条时间戳为结束；VTT/SRT 使用原始 cue 起止；无效 cue 跳过并报告数量，不让整份字幕失效。
- 每个字幕/歌词轨都必须标记来源：`official`（官方歌词/字幕）、`ai`（AI 歌词/字幕）或 `unknown`。作品或音轨卡片在封面角标处显示“官方歌词”或“AI 歌词”；没有可用字幕则不显示角标。

### 播放页体验

- 点击本地音轨即可播放；播放器显示播放/暂停、进度、当前曲目和作品。
- 当前正在播放的音轨在悬浮播放器中提供“字幕/歌词”入口；点击后打开与该音轨绑定的字幕面板，而不是泛用作品文本。
- 字幕面板随播放进度高亮当前句，点击任一句必须将音频 `currentTime` 跳转到该 cue 的 `startMs` 并继续播放；支持切换同一音轨的多个字幕来源与语言。
- 没有匹配字幕时，入口显示禁用/空状态说明；不能展示其他音轨的字幕，也不能使用静态假歌词。
- 桌面歌词需要单独实现：纯浏览器网页只能提供可弹出的歌词窗口，不能可靠地成为系统级置顶浮层。若需求是跨应用、始终置顶的真桌面歌词，必须将桌面端封装为 Tauri 或 Electron，并以原生 always-on-top 透明窗口实现；该窗口与主播放器共享播放进度但不直接暴露本地目录。

## 标签

- 支持作品级与音轨级标签。用户可新增、删除、筛选标签；同步来的题材标签与用户自定义标签需区分来源。
- “官方歌词”“AI 歌词”属于字幕来源标签，优先显示在封面或卡片右上角，使用小而清晰的非交互状态标记；不能把 AI 生成内容标为官方。

## 测试与安全

- 当前本地测试服务以 `DISABLE_AUTH=true` 启动，因此打开页面不需要输入密码。
- 正式部署必须移除该环境变量，并设置强 `ACCESS_PASSWORD`、HTTPS 与安全 Cookie；免认证模式不得用于公网。
- 所有修改接口继续保留 CSRF 校验；测试免认证模式使用固定测试令牌，只用于本机验证。

## 验收

- 桌面首屏可见固定毛玻璃侧栏、两条 4:3 横向媒体轨和居中悬浮播放器。
- 向左或向右滚动任一轨时，卡片可在侧栏后方连续通过；侧栏仍保持可读的模糊遮罩。
- 封面无明显放大模糊，卡片和播放器的悬停/过渡自然且可在减少动态效果设置中关闭。
- 本地服务在测试模式下可直接进入；生产安全开关有明确恢复路径。
- 扫描目录后可播放本地音频；可识别并关联 LRC/VTT/SRT，歌词面板能随播放高亮。
- RJ 编号可分别同步 DLsite 元数据与 ASMR.ONE 中文标题；语言前缀自动清洗，手动标题永远优先。
- 作品/音轨可显示“官方歌词”或“AI 歌词”来源角标；真系统级桌面歌词仅在桌面容器中实现。
